import { TypeItem } from './BaseEnum';
import { SetConfig } from './BaseInterface';

export let measurementParametersConfig: SetConfig = {
  type: [
    {
      name: TypeItem.RANGESELECTION,
      params: [

      ]
    }
  ]
}