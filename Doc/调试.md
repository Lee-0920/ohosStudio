//查询可用设备
```shell
hdc list targets
```

//设置默认网络调试端口(USB连接下)
```shell
hdc tmode port 8710
```

//查询ip
```shell
hdc shell
ifconfig
```

//建立网络连接(可使用网络调试)，wifi或者有线网络
```shell
hdc tconn 192.168.0.209:8710
```
